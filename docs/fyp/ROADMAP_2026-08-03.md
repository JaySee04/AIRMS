# AIRMS Roadmap & Checklist — opened 2026-08-03

Persistent worklist for the large batch JC handed over on 2026-08-03 (two
messages, combined). Survives session resets — **update the status boxes as work
lands.** Nothing here is built yet unless its box is ticked.

Status: `[ ]` not started · `[~]` in progress · `[x]` done · `[?]` blocked on a decision/resource · `[!]` flagged concern

---

## A. Ingestion & athlete identity

- `[x]` **A1 — Filename → name local autofill. DONE (2026-08-03).** `parseNameFromFilename`
  in [`screeningUploadStore.ts`](../../frontend/src/lib/screeningUploadStore.ts) reads the
  athlete name from the LOCAL browser filename (never sent to the model — only redacted
  images are), and a unique roster name-match pre-attaches the athlete after extraction;
  otherwise the operator picks manually. Strips a leading `rpt_<date>_` and a trailing
  `_<hash|phone>`. Verified on both sample shapes + a plain `Name.pdf`. Uploader copy
  updated to say reports are matched by filename where possible.
- `[x]` **A2 — "Replace athlete ID with IC number." COMPLETE (2026-08-04).** Full-send
  confirmed by JC (reseed+PII understood). **STAGE 1:** the athlete
  key's VALUES are now IC numbers (least-risk faithful replace — the PK/FK column
  stays wired, so no rename-everything breakage; each athlete is identified by a
  12-digit IC). Seeder generates deterministic fake ICs (`icFor`), reseeded;
  verified the app runs IC-keyed end-to-end (session / dashboard / squad, `_id` =
  IC). **STAGE 2:** UI relabelled "Athlete ID" → "IC Number" everywhere the operator
  sees/enters it (athlete profile, ProfileShell, import field, medical search,
  AthleteSearchSelect) + a 12-digit IC hint on the import entry field; CLAUDE.md
  demo table updated. Original concern kept
  for the record: Malaysian IC is *sensitive national PII*; `athleteId`
  ("ATH0001") is a synthetic surrogate key that is deliberately PII-free and is the
  canonical FK across **every** table, is serialised as `_id`, and appears in URLs,
  PDF filenames, and logs. Making IC the primary/foreign key sprays a national
  identifier across the entire surface — the *opposite* of the redaction/minimisation
  we just shipped, and a locked-schema change (CLAUDE.md §Locked decisions).
  **Recommendation: keep `athleteId` as the surrogate key; store `icNumber` as a
  protected attribute on `Athlete`** (shown only where needed, access-controlled,
  never in URLs/filenames/logs). Delivers "IC in the system" without the blast
  radius, and is the defensible viva answer (surrogate keys for PII protection).
- `[x]` **A3 — ISN directory integration (MOCK). DONE (2026-08-04).** JC asked for a
  mock ISN database to demonstrate the flow (real names/connection to come). Built
  [`mock/isnDirectory.js`](../../backend/src/mock/isnDirectory.js) — a stand-in for
  ISN's athlete master DB and the **integration seam** (`searchIsn`/`getIsnByIC`
  swap to the real ISN source without touching routes/UI); full record shape (IC,
  name, DOB, derived age, gender, sport, programme, disciplines, nationality, state,
  contacts, registration, status). Routes `GET /api/isn/athletes?q=` (name/IC search,
  each result annotated `inRoster`) + `/:ic`. Frontend
  [`IsnLookup`](../../frontend/src/components/upload/IsnLookup.tsx) in the import
  new-athlete path pre-fills IC/name/sport/programme/age/gender/events from ISN.
  Seeded with 6 ISN-only athletes — including **Mohamed Elffie Danish Bin Khir Johari** (18, Badminton/PELAPIS, IC `080319101817`), added 2026-08-08 as the walkthrough record for the "athlete AIRMS has never seen" path: he is in ISN and deliberately NOT on the AIRMS roster, so a lookup returns `inRoster:false` and he can be created pre-filled in one step. **Add more in the "ADD RECORDS HERE" block.** Verified search + by-IC. **TODO when ISN provides access:**
  point the seam at the real DB/API.

## B. Cohort norms (Module 4)

- `[x]` **B1 — Norm history / versioning. DONE (2026-08-03).** New
  `cohort_norm_versions` table + [`CohortNormVersion`](../../backend/src/models/CohortNormVersion.js)
  model (label, note, createdBy, `snapshot` JSON of every cohort's n/stats/overrides/
  status). Endpoints on the cohorts route: `POST /versions` (save named snapshot),
  `GET /versions` (list), `PATCH /versions/:id` (rename), `POST /versions/:id/restore`
  (admin — upsert the snapshot back onto live cohorts + re-score), `DELETE /versions/:id`
  (admin). UI: a "Saved norm versions" card on the Cohort Norms page (name + save,
  list with Rename / Restore / Delete; restore/delete gated to admin). Verified the
  full lifecycle live (save 40 cohorts → rename → restore + re-score 59 → delete).
- `[x]` **B2 — Discipline-level norms. DONE (2026-08-04).** New **`spgd`** tier
  (sport+programme+gender+discipline) as the **most specific**, then `spgd → spg →
  sg → s → all`. A multi-discipline athlete contributes to *each* of their
  discipline cohorts (computation) and, for scoring, resolves to the first
  qualifying one, else falls back. Threaded discipline through the correctness-
  critical scorer (`cohortKeyOf` central key helper; `tierKeysFor`, `resolveFromMap`,
  and `overallIndicator`'s `resolvedCohortId`/`belongsToCohort`). Schema applied
  non-destructively (new `discipline` column, `spgd` enum value, rebuilt unique
  index; a fresh clone gets it from the model on `npm run seed`). Admin cohort
  labels show the discipline. **Verified zero regression** — bands unchanged
  (28/13/18), all 59 scored, and 13 `spgd` cohorts form (small → fall back to spg
  today; they'll norm by discipline once a discipline group reaches min-n).
- `[~]` **B3/B4/B5 — Unified norm-membership model. BACKEND DONE (2026-08-03).**
  One eligibility resolver (`isEligibleForNorms` in [`utils/cohorts.js`](../../backend/src/utils/cohorts.js),
  used by `recomputeCohorts`): **injured (B4) → manually excluded (B3) → below an
  admin threshold (B5)**; eligibility affects norm COMPUTATION only (everyone is
  still scored). New `Athlete` cols `is_injured`/`injury_note`/`injury_by`/`injury_at`
  + `norm_excluded` (schema applied via `alter` to the dev DB; a fresh clone gets
  them on `npm run seed`). New settings `norm_min_total`/`norm_min_rom`/`norm_min_stability`
  (default 0 = no gate). Endpoints: `GET /api/cohorts/:id/members` (athletes + data
  + eligibility+reason), `PATCH /api/cohorts/members/:athleteId {normExcluded}` (B3),
  `PATCH /api/athletes/:id/injury {isInjured,note}` (B4, medical/admin). Verified:
  threshold + injury toggles flip eligibility correctly.
  **FRONTEND DONE (2026-08-03):** (a) admin per-cohort **Members** panel on the
  Cohort Norms page — each cohort expands to its athletes with Total/ROM/Stability
  score bars, an **In** tick box (manual include/exclude → `normExcluded`), band
  status dot, an eligibility/reason badge, and an **Injured** toggle; changes apply
  on Recompute. (b) medical-side [`InjuryStatusControl`](../../frontend/src/components/dashboard/InjuryStatusControl.tsx)
  on the athlete detail (mark injured/recovered + note, separate from the band).
  (c) B5 threshold knobs (`norm_min_total/rom/stability`) on the admin Settings page.
  **B3/B4/B5 COMPLETE.**
- `[!]` **B design note:** B3 (manual selection) + B4 (injury) + B5 (thresholds)
  all decide the SAME "who is in this cohort's calculation" set. **Build ONE coherent
  membership model** (an effective include/exclude resolver with a reason:
  manual-out / injured / below-threshold), not three parallel mechanisms.

## C. Screening history & cross-athlete views

- `[x]` **C1 — DB records screening time.** Already done: `screenings.assessed_at`
  is populated at commit from the report's "time" value, indexed on
  `(athlete_id, assessed_at)`, and every import writes a full history row
  ([`models/Screening.js`](../../backend/src/models/Screening.js)). No change needed.
- `[x]` **C2 — History section. DONE (2026-08-03), all three roles.** Main dashboard
  shows the LATEST; history lets you pick a past screening by `assessedAt`. Backend
  `GET /api/screenings/:id/full` reshapes any screening into the dashboard shape
  (same access control). Athlete: new `/athlete/history` page + Sidebar link.
  Medical + coach: shared [`ScreeningDatePicker`](../../frontend/src/components/dashboard/ScreeningDatePicker.tsx)
  in their athlete-detail views — picking a past screening swaps the hero/radar/
  panel/body-map to that screening (`view = picked ?? latest`); the clinician band
  override stays bound to the LATEST and hides while viewing history.
- `[x]` **C3 — Athlete views teammates. DONE (2026-08-04).** Scope chosen (note for
  viva): a **same-sport squad readiness summary**, read-only — each teammate's
  programme, overall risk band, and cohort-normed indicator, and **no** peer
  clinical/screening detail (privacy). Backend `GET /api/athletes/teammates`
  (athlete-only, scoped to the caller's sport); new `/athlete/squad` page + Sidebar
  "My Squad". Screenshot-verified (Safe/Needs-attention/Immediate tiles + band-dot
  table, self highlighted), no console errors.

## D. Bug — "reader/API stops working after the site is idle too long"

- `[x]` **D1 — Defensive fixes applied (2026-08-03)** for both top suspects, since
  the exact error wasn't captured yet:
  1. **Tesseract worker timeout + recycle** ([`utils/redactName.js`](../../backend/src/utils/redactName.js)):
     `recognize()` now has a 20s hard timeout and the worker is torn down + rebuilt
     on timeout/error, so a stale worker can't hang the preview (fails closed to the
     fallback box). This is the most likely cause and the one that matches "the PDF
     reader stops."
  2. **MySQL pool + keep-alive + retry** ([`config/db.js`](../../backend/src/config/db.js)):
     `enableKeepAlive`, an explicit `pool` (idle/evict before MySQL's wait_timeout),
     and `retry` on connection-lost error patterns, so a dropped idle connection
     reconnects instead of erroring on the first request after a quiet spell.
- `[~]` **D2 — Confirm.** Still want the backend-terminal error IF it recurs after
  idle, to be sure we fixed the right thing. If it never recurs, done.

## E. Admin dashboard visual upgrade

- `[x]` **E1 — Admin dashboard viz upgrade. DONE (2026-08-04).** Rebuilt the five
  Chart.js charts as plain, theme-aware HTML/CSS (Chart.js dropped from the page):
  the two **pies → 100%-stacked distribution bars** (band distribution, momentum)
  with counts + %; the vertical **score bars → horizontal labelled bars** on a
  0–100 track; **indicators → per-indicator 100%-stacked bars** sorted most-elevated
  first with an elevated-count callout; muscles → labelled horizontal count bars.
  Every value is directly labelled (never colour-alone); status colours reserved.
  Screenshot-verified (light), zero console errors. Built per the `dataviz` skill.

## F. Research — vision cost & capacity (answered; no build)

Measured live against `gemini-flash-lite-latest` on both sample PDFs:

- **Tokens per PDF ≈ 8,250** (prompt+images ≈ **7,466**, output ≈ **780**). Stable
  across files because we always send **6 page-images** at render scale 2 + the fixed
  extraction prompt. Bigger/smaller reports barely move it (image count is fixed).
- **Latency: highly variable on free tier** — measured 6s early, then **88s and 107s**
  later the same day. Plan for tens-of-seconds, occasionally >100s per file.
- **Draining the Gemini free tier:** the binding limit is **requests/day (RPD)**, not
  tokens. At ~8.25k tokens/file even a 250k-TPM/min budget isn't the wall; the daily
  request cap is. **Exact free-tier RPM/RPD/TPM for this model change over time — JC
  should confirm at the AI Studio rate-limits page.** Framework: files/day ≈ RPD;
  tokens/day ≈ RPD × 8.25k.
- **Files at once / current capacity:** no hard cap in code. Frontend queues all
  dropped PDFs and extracts **sequentially with 3s spacing**; backend takes **one
  file per request** (20 MB cap each, in-memory). So a batch of N takes ≈ N ×
  (latency + 3s) — at 100 files that's ~15 min (fast) to ~3 hr (slow) and ~825k
  tokens. **For ISN's "hundreds/day": within reach on free tier IF RPD ≥ that count**,
  but wall-clock time and latency spikes make a paid tier (or multiple keys / a local
  model) the safer path at that volume. **Current 3s spacing ≈ 20 req/min** — verify
  it's under the model's free RPM or bump spacing to avoid 429s on big batches.

---

## G. Consolidation pass (2026-08-06) — no behaviour change

Not part of the original A–F batch. Three places where the same fact lived in
two files and could drift; each now has one owner and, where it's logic, a test.

- `[x]` **G1 — PDF drawing extracted.** `routes/screeningReports.js` had grown to
  ~60 KB holding both *how a report is drawn* and *which route serves it*. All
  pdfkit drawing — palette, `startDoc`/`finish`, `ensure()`, `bar`, `zoneGauge`,
  `radar`, `subitemTable`, `symmetrySection`, `muscleFigure`, the squad
  heatmap/hotspots, and the `interpret()` / `keyFindings()` generators — moved
  **verbatim** to [`utils/pdfDraw.js`](../../backend/src/utils/pdfDraw.js) (765
  lines); the route file is down to 409 lines of routing, fetching and page
  composition. New [`tests/pdfDraw.test.js`](../../backend/tests/pdfDraw.test.js)
  renders all three reports against a fake `res` — PDF changes now have smoke
  coverage with no DB.
- `[x]` **G2 — Body map speaks the HoloMotion vocabulary.** Muscle Flags mode
  draws the **22 individual muscles** instead of the asset's workout regions,
  via [`bodymap-data/muscles.ts`](../../frontend/src/components/dashboard/bodymap-data/muscles.ts).
  16 muscles are recovered from sub-paths the licensed asset already contained;
  6 deep ones are insets derived from their parent's measured bounding box. Sub-paths
  are picked by **measured geometry, not array index** (the asset doesn't order
  left/right limbs identically) and `muscles.test.ts` asserts the relationships
  that would catch a mirror-swap. ROM & Stability mode still draws regions —
  the subitem score genuinely is 5 regions. Asset + MIT attribution untouched;
  only the grain changed. Recorded as [DESIGN_DECISIONS §4a](../DESIGN_DECISIONS.md).
- `[x]` **G3 — Radar axes de-duplicated.** All four dashboards kept private
  `RISK_KEYS` + `RISK_LABEL` + a hard-coded clamp of 30 — four copies of "which
  indicators are shown", each of which had to independently remember to omit
  LDH. Replaced by `RADAR_AXES` / `RADAR_LABELS` / `RADAR_MAX` /
  `riskRadarSeries()` in [`lib/screeningAlerts.ts`](../../frontend/src/lib/screeningAlerts.ts),
  which already owned `INDICATORS`. `RiskRadar.tsx` is now purely presentational.
- `[x]` **G4 — `/athletes/teammates` de-N+1'd.** C3 fetched the latest screening
  once **per squad member**. New `latestIndicatorsFor()` does it in a single
  ordered query off the `(athlete_id, assessed_at)` index, and `INDICATOR_ATTRS`
  stops both paths pulling the big JSON/TEXT columns (`muscle_flags`,
  `summary_text`) and the 12 raw score columns the dashboards never read.
  Mirrors the batching already in `routes/coach.js`.

---

## Decisions needed from JC (blockers)

1. ~~**A2 — IC number**~~ — **DECIDED 2026-08-04:** full replace. The key's values
   are ICs. The privacy concern (a national identifier now appears in URLs, PDF
   filenames and logs) is recorded above and in
   [`MASTER_CLARIFICATIONS.md`](../MASTER_CLARIFICATIONS.md) — **have an answer
   ready for viva**, since the redaction work argues the opposite way.
2. ~~**C3 — teammate visibility scope**~~ — **DECIDED:** same sport, readiness
   summary only (programme + band + indicator), no peer clinical detail.
3. ~~**E1 — admin dashboard** visual direction~~ — **DONE**, built to the `dataviz`
   skill, screenshot-verified.
4. ~~**B2 — discipline** placement in the fallback ladder~~ — **DECIDED:** `spgd`
   sits at the top (most specific), then `spg → sg → s → all`.
5. **Module 2 / FDD rewrite** — the FDD, Table 4.1 and the report still describe
   Injury & Recovery Logging as a live module. **JC's call**: rewrite around the
   surviving clinician flag, or renumber. Do not renumber unilaterally.
6. **D2** — still want the backend-terminal error text if the reader stalls after
   idle again, to confirm D1 fixed the right thing.

## Resources needed

- **ISN DB (A3):** connection details / schema / or an API contract.
- **Bug (D1):** the backend-terminal error text when the reader next fails after idle.
- **Gemini limits (F):** confirm current free-tier RPM/RPD for `gemini-flash-lite-latest`.

## Suggested build order (my recommendation)

1. **D** (bug — small, unblocks daily use) → 2. **C2** (history; data already exists,
high value, low risk) → 3. **B1+B3+B4+B5 as one norms-membership feature** (the big
Module-4 piece; build the unified membership model once) → 4. **A1** (filename autofill,
small) → 5. **E1** (dashboard viz, after a mock) → 6. **B2** (discipline norms) →
**A2/A3** and **C3** pending decisions.
